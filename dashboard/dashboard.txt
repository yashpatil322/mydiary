html:
<?php
session_start();
if (!isset($_SESSION['username'])) {
    header("Location: login.html?error=" . urlencode("Please log in first"));
    exit();
}
$username = $_SESSION['username'];
?>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?php echo htmlspecialchars($username); ?>'s Digital Diary</title>
    
    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@400;600&family=Georgia:ital,wght@0,400;0,700;1,400&display=swap" rel="stylesheet">
    
    <!-- External CSS -->
    <link rel="stylesheet" href="form.css">
    <link rel="stylesheet" href="dashboard.css">
    <script src="dashboard.js"></script>
</head>
<body>
    <!-- Overlay for mobile -->
    <div class="overlay" id="overlay" onclick="closeSidebar()"></div>

    <!-- Enhanced Navbar -->
    <nav class="navbar">
        <div class="menu-icon" onclick="toggleSidebar()" role="button" tabindex="0" aria-label="Toggle navigation menu">
            ☰
        </div>
        <h2><?php echo htmlspecialchars($username); ?>'s Diary</h2>
    </nav>

    <!-- Enhanced Sidebar -->
    <aside class="sidebar" id="sidebar">
        <a href="#" onclick="showDashboard()">📋 Dashboard</a>
        <a href="dairy.php">✍️ New Entry</a>
        <a href="#" onclick="showAllEntries()">📚 All Entries</a>
        <a href="#" onclick="showSettings()">⚙️ Settings</a>
        <a href="#" onclick="showDeleted()">🗑️ Deleted Entries</a>
        <a href="#" onclick="confirmLogout()">🚪 Logout</a>
    </aside>

    <!-- Main Content -->
    <main class="main-content" id="mainContent">
        <!-- Welcome Section -->
        <section class="welcome-section fade-in">
            <h1 class="welcome-title">Welcome back, <?php echo htmlspecialchars($username); ?>!</h1>
            <p class="welcome-subtitle">Ready to capture your thoughts under the watchful eye?</p>
        </section>

        <!-- Stats Section -->
        <section class="stats-container fade-in">
            <div class="stat-card">
                <span class="stat-number" id="totalEntries">12</span>
                <div class="stat-label">Total Entries</div>
            </div>
            <div class="stat-card">
                <span class="stat-number" id="thisMonth">3</span>
                <div class="stat-label">This Month</div>
            </div>
            <div class="stat-card">
                <span class="stat-number" id="currentStreak">5</span>
                <div class="stat-label">Day Streak</div>
            </div>
            <div class="stat-card">
                <span class="stat-number" id="favoriteWord">Happy</span>
                <div class="stat-label">Top Mood</div>
            </div>
        </section>
    </main>
</body>
</html>

css:
:root {
            /* Evil Eye Color Palette */
            --primary-navy: #1a1a2e;
            --secondary-navy: #16213e;
            --evil-eye-blue: #4169e1;
            --dark-purple: #483d8b;
            --steel-blue: #b0c4de;
            --midnight-blue: #000080;
            --paper-cream: #f4f1e8;
            --paper-beige: #e8e2d5;
            --text-dark: #2c3e50;
            --white: #ffffff;
            --shadow-dark: rgba(26, 26, 46, 0.3);
            --shadow-light: rgba(65, 105, 225, 0.2);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Georgia', serif;
            background: linear-gradient(135deg, var(--primary-navy) 0%, var(--secondary-navy) 100%);
            color: var(--text-dark);
            min-height: 100vh;
            overflow-x: hidden;
        }

        /* Background texture */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: 
                radial-gradient(circle at 25% 25%, rgba(255,255,255,0.02) 1px, transparent 1px),
                radial-gradient(circle at 75% 75%, rgba(255,255,255,0.01) 1px, transparent 1px);
            background-size: 50px 50px, 30px 30px;
            pointer-events: none;
            z-index: -1;
        }

        /* Enhanced Navbar */
        .navbar {
            background: linear-gradient(135deg, var(--primary-navy) 0%, var(--dark-purple) 100%);
            color: var(--white);
            padding: 1rem 1.5rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: sticky;
            top: 0;
            z-index: 1000;
            box-shadow: 
                0 4px 20px var(--shadow-dark),
                inset 0 1px 0 rgba(255,255,255,0.1);
            backdrop-filter: blur(10px);
        }

        .navbar h2 {
            margin: 0;
            font-weight: 600;
            font-family: 'Quicksand', sans-serif;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .navbar h2::before {
            content: '📖';
            font-size: 1.5em;
        }

        .menu-icon {
            font-size: 24px;
            cursor: pointer;
            display: block;
            padding: 10px;
            border-radius: 8px;
            transition: all 0.3s ease;
            background: rgba(255,255,255,0.1);
        }

        .menu-icon:hover {
            background: rgba(255,255,255,0.2);
            transform: scale(1.1);
        }

        /* Enhanced Sidebar */
        .sidebar {
            position: fixed;
            top: 0;
            left: -280px;
            width: 280px;
            height: 100vh;
            background: linear-gradient(180deg, var(--primary-navy) 0%, var(--secondary-navy) 100%);
            padding-top: 80px;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            box-shadow: 4px 0 20px var(--shadow-dark);
            z-index: 999;
            backdrop-filter: blur(10px);
        }

        .sidebar::before {
            content: '';
            position: absolute;
            top: 20px;
            left: 50%;
            transform: translateX(-50%);
            width: 60px;
            height: 60px;
            background: radial-gradient(circle, var(--midnight-blue) 20%, var(--evil-eye-blue) 40%, var(--white) 60%, var(--evil-eye-blue) 80%);
            border-radius: 50%;
            border: 3px solid var(--dark-purple);
            box-shadow: 0 0 20px var(--shadow-light);
        }

        .sidebar::after {
            content: '';
            position: absolute;
            top: 40px;
            left: 50%;
            transform: translateX(-50%);
            width: 20px;
            height: 20px;
            background: var(--midnight-blue);
            border-radius: 50%;
        }

        .sidebar a {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 1.2rem 1.5rem;
            color: var(--white);
            text-decoration: none;
            font-weight: 500;
            font-family: 'Quicksand', sans-serif;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .sidebar a::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            width: 0;
            height: 100%;
            background: linear-gradient(90deg, var(--evil-eye-blue), var(--dark-purple));
            transition: width 0.3s ease;
            z-index: -1;
        }

        .sidebar a:hover::before {
            width: 100%;
        }

        .sidebar a:hover {
            color: var(--white);
            transform: translateX(5px);
        }

        .sidebar.show {
            left: 0;
        }

        /* Overlay for mobile */
        .overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.5);
            z-index: 998;
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s ease;
        }

        .overlay.show {
            opacity: 1;
            visibility: visible;
        }

        /* Main Content */
        .main-content {
            transition: margin-left 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            min-height: calc(100vh - 80px);
            padding: 20px;
        }

        /* Welcome Section */
        .welcome-section {
            background: linear-gradient(145deg, var(--paper-cream) 0%, var(--paper-beige) 100%);
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 30px;
            box-shadow: 
                0 10px 30px var(--shadow-dark),
                inset 0 1px 0 rgba(255,255,255,0.8);
            position: relative;
            overflow: hidden;
        }

        .welcome-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-image: 
                radial-gradient(circle at 20% 50%, rgba(139,69,19,0.03) 1px, transparent 1px),
                radial-gradient(circle at 80% 20%, rgba(139,69,19,0.02) 1px, transparent 1px);
            background-size: 40px 40px, 60px 60px;
            pointer-events: none;
        }

        .welcome-title {
            color: var(--text-dark);
            font-size: 2rem;
            font-weight: bold;
            margin-bottom: 10px;
            text-shadow: 1px 1px 2px rgba(0,0,0,0.1);
        }

        .welcome-subtitle {
            color: var(--dark-purple);
            font-size: 1.1rem;
            font-style: italic;
            opacity: 0.8;
        }

        /* Stats Cards */
        .stats-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .stat-card {
            background: linear-gradient(145deg, var(--white) 0%, var(--paper-cream) 100%);
            padding: 25px;
            border-radius: 12px;
            box-shadow: 0 5px 15px var(--shadow-light);
            border: 2px solid var(--steel-blue);
            text-align: center;
            transition: all 0.3s ease;
        }

        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px var(--shadow-light);
            border-color: var(--evil-eye-blue);
        }

        .stat-number {
            font-size: 2rem;
            font-weight: bold;
            color: var(--evil-eye-blue);
            display: block;
        }

        .stat-label {
            color: var(--text-dark);
            font-size: 0.9rem;
            margin-top: 5px;
            opacity: 0.8;
        }

        /* Form Container Enhancement */
        .diary-form-wrapper {
            background: transparent;
            border-radius: 15px;
            overflow: hidden;
        }

        /* Responsive Design */
        @media (min-width: 768px) {
            .menu-icon {
                display: none;
            }

            .sidebar {
                left: 0;
                padding-top: 100px;
            }

            .main-content {
                margin-left: 280px;
            }

            .navbar {
                padding-left: calc(1.5rem + 280px);
            }
        }

        @media (max-width: 767px) {
            .navbar h2 {
                font-size: 1.3rem;
            }

            .welcome-section {
                padding: 20px;
            }

            .welcome-title {
                font-size: 1.6rem;
            }

            .stats-container {
                grid-template-columns: repeat(2, 1fr);
                gap: 15px;
            }

            .stat-card {
                padding: 20px;
            }

            .stat-number {
                font-size: 1.5rem;
            }

            .main-content {
                padding: 15px;
            }
        }

        @media (max-width: 480px) {
            .stats-container {
                grid-template-columns: 1fr;
            }
            
            .navbar {
                padding: 0.8rem 1rem;
            }
            
            .sidebar {
                width: 250px;
                left: -250px;
            }
        }

        /* Loading Animation */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .fade-in {
            animation: fadeInUp 0.6s ease-out;
        }

        /* Scroll Enhancement */
        ::-webkit-scrollbar {
            width: 8px;
        }

        ::-webkit-scrollbar-track {
            background: var(--paper-beige);
        }

        ::-webkit-scrollbar-thumb {
            background: var(--evil-eye-blue);
            border-radius: 4px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: var(--dark-purple);
        }

js:
 // Enhanced JavaScript with better functionality
        
        // Initialize app
        document.addEventListener('DOMContentLoaded', function() {
            initializeApp();
            addAccessibilityFeatures();
            setDefaultDate();
        });

        function initializeApp() {
            // Set today's date as default
            document.getElementById('date').valueAsDate = new Date();
            
            // Add form validation
            setupFormValidation();
            
            // Add auto-save functionality
            setupAutoSave();
            
            // Update stats (placeholder - replace with actual data)
            updateStats();
        }

        function addAccessibilityFeatures() {
            // Add keyboard navigation for mood selector
            document.querySelectorAll('.mood-option').forEach((option, index) => {
                option.setAttribute('tabindex', '0');
                option.addEventListener('keydown', function(e) {
                    if (e.key === 'Enter' || e.key === ' ') {
                        e.preventDefault();
                        const radio = this.querySelector('input[type="radio"]');
                        radio.checked = true;
                        radio.focus();
                    }
                });
            });

            // Add keyboard support for menu icon
            document.querySelector('.menu-icon').addEventListener('keydown', function(e) {
                if (e.key === 'Enter' || e.key === ' ') {
                    e.preventDefault();
                    toggleSidebar();
                }
            });
        }

        function setDefaultDate() {
            document.getElementById('date').valueAsDate = new Date();
        }

        function setupFormValidation() {
            const form = document.getElementById('diaryForm');
            const inputs = form.querySelectorAll('input[required], textarea[required]');
            
            inputs.forEach(input => {
                input.addEventListener('blur', function() {
                    validateField(this);
                });
                
                input.addEventListener('input', function() {
                    if (this.classList.contains('error')) {
                        validateField(this);
                    }
                });
            });
        }

        function validateField(field) {
            const isValid = field.checkValidity();
            field.classList.toggle('error', !isValid);
            
            // Add visual feedback
            if (!isValid) {
                field.style.borderColor = '#e74c3c';
                field.style.boxShadow = '0 0 5px rgba(231, 76, 60, 0.3)';
            } else {
                field.style.borderColor = '';
                field.style.boxShadow = '';
            }
        }

        function setupAutoSave() {
            const form = document.getElementById('diaryForm');
            const inputs = form.querySelectorAll('input, textarea, select');
            
            inputs.forEach(input => {
                input.addEventListener('input', debounce(function() {
                    // Auto-save to localStorage
                    const formData = new FormData(form);
                    const data = Object.fromEntries(formData.entries());
                    localStorage.setItem('diary_draft', JSON.stringify(data));
                }, 1000));
            });

            // Load draft on page load
            loadDraft();
        }

        function loadDraft() {
            const draft = localStorage.getItem('diary_draft');
            if (draft) {
                const data = JSON.parse(draft);
                Object.keys(data).forEach(key => {
                    const field = document.querySelector(`[name="${key}"]`);
                    if (field) {
                        if (field.type === 'radio') {
                            const radio = document.querySelector(`[name="${key}"][value="${data[key]}"]`);
                            if (radio) radio.checked = true;
                        } else {
                            field.value = data[key];
                        }
                    }
                });
            }
        }

        function debounce(func, wait) {
            let timeout;
            return function executedFunction(...args) {
                const later = () => {
                    clearTimeout(timeout);
                    func(...args);
                };
                clearTimeout(timeout);
                timeout = setTimeout(later, wait);
            };
        }

        function updateStats() {
            // Placeholder stats - replace with actual database queries
            document.getElementById('totalEntries').textContent = '12';
            document.getElementById('thisMonth').textContent = '3';
            document.getElementById('currentStreak').textContent = '5';
            document.getElementById('favoriteWord').textContent = 'Happy';
        }

        // Sidebar functionality
        function toggleSidebar() {
            const sidebar = document.getElementById('sidebar');
            const overlay = document.getElementById('overlay');
            const isOpen = sidebar.classList.contains('show');
            
            sidebar.classList.toggle('show', !isOpen);
            overlay.classList.toggle('show', !isOpen);
            
            // Update ARIA attributes
            sidebar.setAttribute('aria-hidden', isOpen);
            document.querySelector('.menu-icon').setAttribute('aria-expanded', !isOpen);
        }

        function closeSidebar() {
            const sidebar = document.getElementById('sidebar');
            const overlay = document.getElementById('overlay');
            
            sidebar.classList.remove('show');
            overlay.classList.remove('show');
            
            sidebar.setAttribute('aria-hidden', 'true');
            document.querySelector('.menu-icon').setAttribute('aria-expanded', 'false');
        }

        // Navigation functions
        function showDashboard() {
            // Implementation for showing dashboard
            console.log('Showing dashboard');
            closeSidebar();
        }

        function showNewEntry() {
            // Scroll to form
            document.querySelector('.diary-container').scrollIntoView({ 
                behavior: 'smooth' 
            });
            closeSidebar();
        }

        function showAllEntries() {
            // Implementation for showing all entries
            console.log('Showing all entries');
            closeSidebar();
        }

        function showSettings() {
            // Implementation for showing settings
            console.log('Showing settings');
            closeSidebar();
        }

        function showDeleted() {
            // Implementation for showing deleted entries
            console.log('Showing deleted entries');
            closeSidebar();
        }

        function confirmLogout() {
            if (confirm('Are you sure you want to logout?')) {
                window.location.href = 'logout.php';
            }
            closeSidebar();
        }

        // Enhanced form submission
        document.getElementById('diaryForm').addEventListener('submit', function(e) {
            e.preventDefault();
            
            const saveBtn = document.getElementById('saveBtn');
            const btnText = document.getElementById('btnText');
            const btnLoader = document.getElementById('btnLoader');
            
            // Show loading state
            saveBtn.disabled = true;
            btnText.style.display = 'none';
            btnLoader.style.display = 'inline';
            
            // Get form data
            const formData = new FormData(this);
            const entry = Object.fromEntries(formData.entries());
            
            // Simulate API call
            setTimeout(() => {
                // Success feedback
                showNotification('Your diary entry has been saved! ✨', 'success');
                
                // Clear draft
                localStorage.removeItem('diary_draft');
                
                // Reset form
                this.reset();
                setDefaultDate();
                
                // Reset button state
                saveBtn.disabled = false;
                btnText.style.display = 'inline';
                btnLoader.style.display = 'none';
                
                // Update stats
                updateStats();
                
                console.log('Diary Entry:', entry);
            }, 1500);
        });

        // Notification system
        function showNotification(message, type = 'info') {
            const notification = document.createElement('div');
            notification.className = `notification ${type}`;
            notification.textContent = message;
            notification.style.cssText = `
                position: fixed;
                top: 100px;
                right: 20px;
                background: ${type === 'success' ? '#27ae60' : '#3498db'};
                color: white;
                padding: 15px 20px;
                border-radius: 8px;
                box-shadow: 0 5px 15px rgba(0,0,0,0.3);
                z-index: 10000;
                animation: slideInRight 0.3s ease;
            `;
            
            document.body.appendChild(notification);
            
            setTimeout(() => {
                notification.style.animation = 'slideOutRight 0.3s ease';
                setTimeout(() => {
                    document.body.removeChild(notification);
                }, 300);
            }, 3000);
        }

        // Make mood selection more interactive
        document.querySelectorAll('.mood-option').forEach(option => {
            option.addEventListener('click', function() {
                // Remove active class from all options
                document.querySelectorAll('.mood-option').forEach(opt => {
                    opt.classList.remove('active');
                });
                
                // Add active class to clicked option
                this.classList.add('active');
                
                const radio = this.querySelector('input[type="radio"]');
                radio.checked = true;
            });
        });

        // Close sidebar on escape key
        document.addEventListener('keydown', function(e) {
            if (e.key === 'Escape') {
                closeSidebar();
            }
        });

        // Add CSS animations
        const style = document.createElement('style');
        style.textContent = `
            @keyframes slideInRight {
                from { transform: translateX(100%); opacity: 0; }
                to { transform: translateX(0); opacity: 1; }
            }
            
            @keyframes slideOutRight {
                from { transform: translateX(0); opacity: 1; }
                to { transform: translateX(100%); opacity: 0; }
            }
            
            .mood-option.active {
                background: rgba(65,105,225,0.2) !important;
                border-color: #4169e1 !important;
                transform: scale(1.02);
            }
            
            .form-group input.error,
            .form-group textarea.error {
                border-color: #e74c3c !important;
                box-shadow: 0 0 5px rgba(231, 76, 60, 0.3) !important;
            }
        `;
        document.head.appendChild(style);